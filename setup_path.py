import os
from os.path import join, realpath, dirname

BASE_PATH = dirname(realpath(__file__))
PARAMS_PATH = join(BASE_PATH, 'yaml')
RUN_PATH = join(BASE_PATH, 'run')

DATA_PATH='/home/haitham'
OUTPUT_PATH='/home/haitham/mnt/__output_v2'

