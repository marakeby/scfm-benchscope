# Discovered model candidates

The discovery agent writes one JSON file per model under date folders here.
These files are evidence only. They are not install recipes and not approval
to run a job.

See [Model candidates](../integration/model-candidates.md) for the format
and [Integration agent](../integration/README.md) for the next steps.
