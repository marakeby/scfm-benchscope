run_id: 
dataset:
    - luad1/dataset_shared.yaml

embedding:
    method: scVI
    module: features.scvi_extractor
    class: scVIEmbeddingExtractor
    viz: False
    eval: False
    params:
        batch_key: sample_id
        n_layers: 2
        n_latent: 100
        gene_likelihood: "nb"
        max_epochs: 10
        layer_name: #None 

