# Discovered model candidates

The scheduled discovery agent writes immutable, validated candidate JSON files
below date-based directories in this folder. These files are automatically
published discovery evidence, not approved installation or execution plans.
