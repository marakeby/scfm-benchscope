run_id: 
dataset:
    - brca_full/pre_post/dataset_h5ad_tcells_only.yaml

classification:
    - brca_full/pre_post/classification_randomforest_pre_post.yaml

preprocessing:
    skip: True

hvg:
    skip: True

embedding:
    load_cached: False
    method: scimilarity
    module: features.scimilarity_extractor
    class: SCimilarityExtractor
    viz: True
    eval: True
    params: 
        model_path: /home/jupyter/mnt/MODELS/SCimilarity/model_v1.1
        use_gpu: True

